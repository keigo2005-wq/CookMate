# アプリ企画メモ（CookMate）

## 1. 背景
一人暮らしで料理経験が少ないため、食生活が栄養的に偏りやすいです。

## 2. 解決したい問題
冷蔵庫の食材から「何を作るか」を決めるのに時間がかかります。

## 3. 想定する利用者
まずはけいごさん本人です。将来的には同じ悩みを持つ一人暮らしの学生も想定できます。

## 4. アプリで実現したいこと
食材・時間・栄養の条件を入力すると、合うレシピが分かりやすく出てきます。

入力例：
- 鶏むね肉、キャベツ、卵が余っている
- 20分以内で作りたい
- 野菜を多めに取りたい

出力する情報：
- 条件に合うレシピ
- 余っている食材をどの程度使えるか
- 追加で必要な食材
- 調理時間
- 栄養面での特徴
- レシピ内容の分かりやすい説明
- レシピの出典

## 5. AIを使う理由
条件に合わせた説明や要約を、自然な文章で生成できるからです。

## 6. RAGを使う理由
LLM単体だと存在しないレシピを作ってしまう可能性があります。
実在するレシピデータを検索し、それを根拠に回答させるためです。

処理の流れ：
1. ユーザーの入力
2. 入力内容から条件を整理
3. レシピデータから関連するレシピを検索
4. 検索結果をLLMに渡す
5. LLMがユーザーに分かりやすく回答
6. レシピの出典も表示

## 7. 最初に作るべき最低限の機能（MVP）
- 食材・時間の条件を入力する
- レシピデータから条件に合うものを検索する
- LLMが検索結果を分かりやすくまとめて回答する
- 出典レシピを表示する

## 8. 将来的に追加できる機能
- 栄養バランスの数値評価
- 複数レシピの提案
- 献立の週単位管理

## 9. この開発を通して身につけるべき知識
- LLM APIの使い方
- RAGの仕組み
- プロンプト設計
- 回答の評価方法

## 決定事項
- サービス名：CookMate（料理の相棒）
- レシピデータ：自作データ（CookMateオリジナル、60件）
- 操作方法：簡単なWeb画面（Streamlit）
- LLM API：Anthropic Claude API
- 保存場所：ai-work/cookmate

## レシピデータの項目
- レシピ名／材料（1人分・分量つき）／調理時間／カテゴリ／タグ
- 作り方／栄養の特徴／価格目安／難易度／保存・作り置き／出典

## レシピデータの調査メモ
楽天レシピ・クックパッドの公開データセットは、NII（国立情報学研究所）への
申請が必要で、GitHubへのデータ再配布が禁止されている。
GitHub公開を目指す本プロジェクトの方針と合わないため、自作データを採用した。

## データ作成状況
- data/recipes.json に60件を保存済み（主菜22／副菜17／汁物11／主食10）
- data/ingredient_prices.json に食材ごとの目安価格（88品目）を保存済み

## 実装状況（MVP完成後の改善フェーズ）
- search.py：食材・時間・カテゴリでの検索、費用見積もり、
  「あと少しで作れるレシピ」の検索ロジックを実装
- llm.py：Claude APIへの問い合わせ、候補レシピと近いレシピの両方を
  プロンプトに含めて回答を生成
- app.py：Streamlitによる画面。暖色系のデザイン、カテゴリごとの
  カラーバッジアイコン、タグでの絞り込み、費用表示に対応

## ユーザーからのフィードバックと対応（2026-09-19）
1. デザインをポップに → 暖色系テーマ＋カード型デザインに変更
2. 条件の選択肢を増やす → 「節約」「作り置き向き」タグとカテゴリ絞り込みを追加
3. 「あと少しで作れる」を強調 → 専用セクションを新設
4. レシピ数を増やす → 36件→60件に拡充
5. 完成品・材料の画像 → 実写真はデータに存在しないため、
   料理名から判定するアイコン（丸いカラーバッジ）で代替
6. 追加食材の費用表示 → 食材ごとの目安価格データを追加し表示
7. 難易度表示 → 星（★）でカードに表示

## ユーザーからのフィードバックと対応（2026-09-19 その2）
1. タイトルに副題が欲しい → 「〜あなたの冷蔵庫の、料理の相棒〜」を追加
2. 食材をカテゴリ分けしたい → 肉類／魚介類／卵・乳製品／豆腐・大豆製品／
   野菜／きのこ類／ご飯・麺・パン／調味料／その他の9分類で選択欄を分割
   （data/ingredient_categories.json）
3. 並び替え機能が欲しい → 時間・費用・難易度で並び替えるプルダウンを追加
   （検索結果はセッションに保持し、並び替えのみで再検索・再AI生成しない設計）
4. 分量・手順が大雑把 → 「使える食材」に分量を表示。全60件の手順を、
   火加減・時間の目安・仕上がりのサイン（色が変わったら等）を含めて書き直した
5. ×ボタンで選択解除できない → 自由入力可能なmultiselect（accept_new_options）が
   原因だった可能性があり、カテゴリ別のプレーンなmultiselectに変更して解消

### 開発中に見つけて直したバグ
- カードのHTMLが、PythonのインデントとMarkdownのコードブロック記法が
  衝突してそのまま文字表示される不具合 → f文字列を1行にまとめて解消
- 「水」が採点に加算され、本来1位になるべきレシピが2位になる不具合 →
  常備品は加点・お勧めバッジ判定から除外するよう修正
- AIコメントと「⭐一番のおすすめ」バッジが別のレシピを指してしまう不具合 →
  バッジ対象のレシピ名をプロンプトで明示し、AIがそれ以外を推薦しないよう指示

## ユーザーからのフィードバックと対応（2026-09-19 その3）
1. 食材の選択肢が重複している → ひらがな/カタカナ違いの重複表記、
   「豚肉」「牛肉」のような総称語を選択肢から削除（86品目→整理後）
2. レシピを100件に増やしたい → 40件を新規作成し、60件→100件に拡充
   （主菜34／副菜29／汁物19／主食18）。新しい食材20品目を追加し、
   カテゴリ・価格データも整備した
3. 作り方にアレンジ方法を書いてほしい → 全100件に「アレンジ方法」を追加し、
   「作り方を見る」の最後に表示するようにした
4. 「その他」カテゴリが分かりにくい → カテゴリを廃止し、キムチ・梅干しは
   野菜、ちくわは魚介類へ振り分けた。自由入力欄のプレースホルダーも、
   実在の選択肢と被らない例に変更した
5. イラストの解像度を上げてほしい、レシピごとに変えてほしい →
   料理の種類ごとの土台イラストに、(1)レシピIDによる色合いの
   バリエーション、(2)実際の材料から選ぶ食材アイコン（トマト・にんじん・
   卵など）を組み合わせる仕組みに作り直した。自作のSVGのみで無料。

### 開発中に見つけて直したバグ・データの不整合
- 「めんつゆ」表記が一部レシピだけ「麺つゆ」になっており、
  正しく検索一致しない不整合があった → 表記を統一
- 横長のイラスト枠に対し、SVGの表示モード（slice）の仕様で
  画像の上下が切り取られ、食材アイコンが見えなくなっていた →
  切り取られない安全な位置にアイコンを配置し直した

## 技術要素の追加（2026-09-19 その4）：埋め込みベクトル検索

就職活動でのアピール材料として、AI開発の技術要素を増やす方針を確認。
提案した案のうち、次の3つを実施することに決定：
1. 埋め込みベクトル検索（実施済み）
2. お気に入り保存機能（実施済み）
3. 栄養バランスの数値化（実施済み）

### お気に入り保存機能の実装
- `favorites.py`：お気に入りレシピIDの読み込み・追加・削除
- `data/favorites.json`：お気に入りIDのリストを保存するファイル
  （データベースを使うほどの規模ではないため、シンプルなファイル保存を採用）
- 画面を「🔍 レシピを探す」「❤️ お気に入り」の2タブ構成に変更
- 各レシピカードに🤍/💔ボタンを追加し、その場で追加・解除できるようにした

### 栄養バランスの数値化（カロリー・PFC）の実装
- `data/ingredient_nutrition.json`：食材ごとの、レシピで使う分量あたりの
  カロリー・たんぱく質・脂質・炭水化物の目安値（107品目、文部科学省の
  食品成分表を参考にした概算値）
- `search.py`の`calculate_nutrition()`：レシピの材料を合計してカロリーと
  PFCバランス(%)を計算
- 各レシピカードに「🔥 目安◯kcal」と、PFCバランスを表す色分けバーを表示

### 埋め込みベクトル検索の実装
- `embeddings.py`：文章を埋め込みベクトルに変換する処理
  - モデル：intfloat/multilingual-e5-small（無料・ローカル実行、
    Hugging Face経由でパソコンにダウンロードして使う）
  - Claude APIには埋め込み機能が無いため、この方式を採用
- 画面に「こんな料理が食べたい（自由記述）」欄を追加。食材を選ばなくても、
  意味の近いレシピを検索できるようになった
- `search.py`の`search_recipes`に`query_text`引数を追加し、
  食材の一致スコアと意味の近さのスコアを合算するハイブリッド検索に設計

### ベクトルデータベース（Chroma）への移行（2026-09-19 その5）
埋め込みベクトルの保存・類似度計算を、自作のJSON+Python計算から
専用のベクトルデータベース「Chroma」（無料・ローカル動作）に移行した。
- `vector_store.py`：Chromaへのレシピ登録・検索を担当
  （`embeddings.py`は埋め込みベクトルへの変換だけを担当する形に整理）
- `data/chroma_db/`：Chromaのデータベース本体（.gitignoreに追加。
  recipes.jsonから自動生成されるため、GitHubには含めない）

### レシピデータ変更時の注意
新しいレシピを追加・編集したときは、`python vector_store.py` を
再実行してベクトルデータベースを作り直す必要がある。

### 週間献立プランナー＋買い物リスト自動生成の実装
- `meal_plan.py`：曜日ごとのレシピ割り当てを`data/meal_plan.json`に保存
- 画面に「📅 週間献立」タブを追加。月〜日にレシピを選ぶと、
  食材をまとめた買い物リストと、合計費用・カロリーが自動表示される
- 同じ食材が複数レシピで使われる場合は、分量を並べて表示する方式を採用
  （単位がバラバラな分量を正確に合算するのは難しいため）

### AIによる週間献立の自動設計（2026-09-20）
想定ユーザーの大学生は、栄養バランスの良い献立の組み方が分からないと
想定されるため、AIに1週間分の献立を設計させる機能を追加。

- 技術要素：Tool Use（Function Calling）を使用。AIに自由な文章で
  答えさせるのではなく、「assign_weekly_plan」という決まった形（曜日→
  レシピID）でデータを返させる仕組み。これにより、AIの回答をそのまま
  安全にアプリのデータとして扱える
- `llm.py`の`generate_weekly_plan()`：全レシピの栄養データをAIに渡し、
  1週間・1日1品で、たんぱく質・脂質・炭水化物や食材が偏らないよう
  選んでもらう。選んだ理由も一緒に返す
- 方針：「1日の合計カロリー管理」ではなく、「1週間を通して品目が
  偏らないようにする」という範囲に絞った（1日1品のデータ構造のため）
- 安全対策：AIが返した内容を必ず検証する（全曜日そろっているか、
  存在するレシピIDか、重複が無いか）。不正な形式のときは反映しない

### 週の目標に応じた献立設計（2026-09-20）
「高たんぱくを摂りたい週」「カロリー控えめにしたい週」など、
ユーザーの要望によって献立を変えられるようにした。
- 画面にクイック選択（高たんぱく重視／カロリー控えめ／野菜多め／
  時短重視／節約重視）と、自由記述欄を追加
- `generate_weekly_plan()`に`user_goal`引数を追加し、要望がある場合は
  栄養バランス重視のルールより要望を優先するようプロンプトを調整

**評価で見つけたAIの回答品質の問題**：テスト中、AIが返す「理由の説明文」が、
実際に選んだレシピと異なるレシピ名を挙げてしまう不整合を発見した
（Tool Useの構造化データ部分は正しいのに、説明文だけ別のレシピを
語ってしまう現象）。プロンプトに「説明文で挙げるレシピ名は、必ず
実際に選んだものと一致させ、書く前に見直すこと」という指示を追加して
再現しなくなることを確認した。AIの出力は構造化データが正しくても
説明文は別チェックが要る、という学びが得られた。

### ユーザーからのフィードバックと対応（2026-09-20 その2）
1. AIで献立設計しても、割り当ても買い物リストも反映されなかった →
   Streamlitの仕様上のバグを発見。曜日の選択欄(selectbox)は、一度
   表示されるとウィジェット自身の状態を持ち続け、次の実行時にそちら が
   優先されてしまうため、AIが決めた内容で裏のファイルを書き換えても、
   画面には反映されない（むしろ古い選択欄の状態でファイルが
   上書きされてしまう）という不具合だった。AIが献立を決めた直後に、
   選択欄の状態(st.session_state)も一緒に書き換えるよう修正した
2. 自分でレシピを選んだ後、除去する✕マークが分かりにくい →
   各曜日に「✕（未定に戻す）」ボタンを常設し、確実に消せるようにした
   （実装時、ボタンのクリック処理でst.session_stateを直接書き換えると
   「ウィジェット作成後に書き換えできない」というエラーが発生したため、
   on_clickコールバックを使う形に修正した）
3. 「こんな料理が食べたい」欄のヘルプ文にある「AI(埋め込みベクトル)」の
   括弧書きが、利用者には分かりにくいため削除し、「AIが探します」に変更

### ユーザーからのフィードバックと対応（2026-09-20 その3）
1. 選んだ選択肢を一括でリセットするボタンが欲しい →
   検索画面に「🔄 条件をリセット」、週間献立に
   「🔄 献立をすべて未定に戻す」ボタンを追加（個別の解除方法に加えて）
2. PFCを円グラフで、右側に示してほしい →
   横棒グラフから、CSSのconic-gradientを使った円グラフに変更し、
   カード内でテキストの右側に配置
3. 材料費の計算が合っていない気がする →
   食材ごとの目安価格を全体的に見直し（例:鶏むね肉100円→130円、
   豚こま肉150円→190円、缶詰・豆腐など「1個/1丁を買う必要がある」
   食材は特に引き上げ）。各レシピの価格目安も、更新した単価をもとに
   再計算した
4. 献立を組んでもらった画面でもレシピの詳細を見たい →
   週間献立タブに「📋 献立の詳細」を追加し、割り当てた曜日ごとに
   通常の検索結果と同じカード（イラスト・栄養・作り方）を表示
5. 検索結果表示後に選択肢を解除したら、結果も連動して変わってほしい →
   検索欄を囲んでいた`st.form`を廃止し、条件を変えるたびに即座に
   再検索する設計に変更。ただしAIのコメント生成はClaude API呼び出しの
   コストがかかるため、「🤖 AIのおすすめコメントをもらう」ボタンを押した
   ときだけ生成する方式にした（条件が変わったら、古いコメントは
   自動的に非表示にする）

### 開発中に見つけて直した問題
- Hugging Faceへの接続で証明書エラーが発生 →
  `pip-system-certs`で一時対応したが、これがAnthropic API通信で
  無限再帰エラーを起こす副作用があり不採用に。最終的には
  「一度ダウンロードしたモデルは、以後インターネットに確認しに
  行かない」設定（HF_HUB_OFFLINE）で解決し、２つのライブラリが
  競合しない安全な形にした
- 埋め込みモデル（sentence-transformers）を読み込むと、Streamlitの
  自動リロード監視が無関係な大量のモジュールをスキャンして
  極端に重くなる問題があった → 自動リロード監視を無効化して解決
  （今後コードを変更したら、手動でサーバーを再起動する運用に変更）

## マイレシピ（自分で登録するレシピ）機能の実装（2026-09-20 その4）

自分でアレンジして作った料理も記録できるようにしてほしい、という要望を
受けて追加。材料・費用・難易度・所要時間などをユーザー自身が入力する形。

- `custom_recipes.py`：登録したレシピを`data/custom_recipes.json`に
  保存・読み込み・削除する処理。本体のレシピ(`data/recipes.json`)とは
  別ファイルに分け、`custom_`から始まる専用IDを振ることで、
  データが混ざらないようにした
- `search.py`の`load_recipes()`を、本体レシピとマイレシピを
  合わせて返すように変更。これにより、検索・お気に入り・週間献立
  プランナーなど、他の機能からもマイレシピを区別なく扱えるようにした
- `vector_store.py`に`add_recipe_to_index()`・`delete_recipe_from_index()`
  を追加。レシピが1件登録・削除されるたびに、全件を作り直さず
  その1件だけをベクトルデータベースに反映する
- 画面に「📝 マイレシピ」タブを追加。材料は「食材名,分量」の1行1食材、
  手順は1行1手順で入力する形式。費用目安を0のままにした場合は、
  他のレシピと同じく食材の目安価格から自動見積もりされる
- 登録したレシピは通常のレシピと同じカードで表示され、削除ボタンも
  各カードに設置

### 動作確認（ブラウザで実施）
サンプルレシピ（自家製アレンジ豚キャベツ炒め）を登録し、以下を確認した。
- 登録後、一覧に正しくカード表示される（イラスト・PFC円グラフ・
  価格・難易度・手順・アレンジ方法・栄養の特徴すべて反映）
- 食材（豚こま肉）を選ぶ検索でヒットする
- 「こんな料理が食べたい」の自由記述（意味検索）でもヒットする
  →ベクトルデータベースへの個別登録が正しく機能していることを確認
- 週間献立プランナーの曜日選択にも候補として表示され、割り当てると
  献立の詳細・買い物リストにも正しく反映される
- 削除ボタンで一覧から消え、検索結果にも出なくなることを確認
- サーバーエラーは発生しなかった

## ユーザーからのフィードバックと対応（2026-09-20 その5）
1. 週間献立の選択欄に「（未定）」という選択肢は不要 →
   選択肢一覧から「（未定）」を削除し、未選択時はプレースホルダー
   「Choose an option」を表示する形に変更
2. 選択解除ボタンが、選択欄の右端にあるStreamlit標準の✕（黒丸）と、
   その外側に別途配置した独自の✕ボタンの2種類あって分かりにくい →
   独自ボタンを削除し、標準の✕（黒丸）ボタンだけに統一

### 実装で分かったStreamlitの仕様
`st.selectbox`の標準✕（クリア）ボタンは、`index=None`をその実行の
たびに毎回渡し続けないと表示されなくなる。曜日が選択された後の
再実行で`index=`に実際の選択位置（0以上の数値）を渡すと、✕ボタンが
消えてしまうという癖があった。対応として、`index`は常に`None`を渡し、
現在の選択内容は`st.session_state`側（初回のみ保存済みの献立で
初期化し、以降はウィジェットの操作や献立設計AI・リセット機能が
直接書き換える）で管理する設計に変更した。

## 公開・スマホ対応フェーズ（2026-09-20 その6）

GitHub掲載＋スマホからも使えるようにする作業を開始。CookMateは
Streamlit(Python)アプリのため、Netlifyのような静的サイト公開サービスは
使えないと判断し、Streamlit専用の無料公開サービス「Streamlit Community
Cloud」を使う方針にした。

公開すると、URLを知っている人なら誰でもアクセスできてしまうため、
利用者ごとにデータを分けたいという要望を受け、次の対応を行った。

### ログイン機能とデータ保存先の変更（Supabase連携）

- 無料のデータベースサービス「Supabase」に、利用者・お気に入り・
  週間献立・マイレシピの4つの表(テーブル)を作成した
- `auth.py`（新規）：ニックネーム＋パスワードでの新規登録・ログイン処理。
  パスワードはbcryptで不可逆なハッシュに変換してから保存している
  （データベースの中身を見られても元のパスワードは分からない）
- `favorites.py`・`meal_plan.py`・`custom_recipes.py`を、
  ローカルのJSONファイル保存からSupabaseへの保存に書き換えた。
  すべての関数が`user_id`を受け取り、その利用者のデータだけを
  読み書きするようにしたことで、利用者ごとにデータが分かれる
- `search.py`の`load_recipes()`・`search_recipes()`・
  `find_near_miss_recipes()`も`user_id`を受け取るように変更し、
  本人のマイレシピだけを検索対象に含めるようにした
- `app.py`の先頭に、ログインしていない場合はログイン画面だけを
  表示する仕組み（ログインゲート）を追加。ログイン後は
  サイドバーに「👤 ○○さん」とログアウトボタンを表示する
- セキュリティ設計：SupabaseのRLS(行単位セキュリティ)は有効化した上で、
  「アプリ(anonキー)を持っていれば読み書き可能」というシンプルな
  ポリシーにした。本格的な認証(auth.uid()連携)は今回の規模には
  過剰と判断し、利用者ごとの絞り込みはアプリ側のコード(user_id指定)で
  行う設計にした。本名・機密情報を扱わないアプリという前提の上での判断

### 開発中に見つけて直した問題
- SupabaseへのHTTPS通信で証明書エラー(SSL: CERTIFICATE_VERIFY_FAILED)が
  発生 → `truststore`というライブラリを使い、WindowsのOSが信頼している
  証明書をそのまま使う設定に変更して解決した
  （以前Hugging Face接続で使った`pip-system-certs`とは別の仕組みで、
  Anthropic社のライブラリも内部的に使っている実績のある方法を採用した）
- Supabaseのテーブル作成時に「Automatically expose new tables」を
  OFFにしたため、(1)テーブルがAPI経由で見つからない
  (`PGRST205`)、(2)見つかっても読み書き権限が無い(`42501`)、
  という2つのエラーが発生 → `NOTIFY pgrst, 'reload schema';`と、
  `grant select, insert, update, delete on ... to anon;`を
  それぞれ実行して解決した
- `.env`の`SUPABASE_URL`に、Data APIページの「API URL」
  （`/rest/v1/`という通信用パスまで含んだ値）をそのまま貼って
  しまい、パスが二重になるエラー(`PGRST125`)が発生 → プロジェクトの
  URLだけ(`https://xxxxx.supabase.co`)に直して解決した

### 動作確認
- 新規登録・ログイン・ログアウトが正常に動作することを確認
- 「keigo」と「tarou」の2つのアカウントでそれぞれお気に入りを
  操作し、データが別々に保存されることを確認
- 別アカウントでマイレシピを登録し、Supabaseへの保存・
  意味検索用ベクトルデータベースへの登録の両方が成功することを確認

### スマホ対応（2026-09-20 その7）

- Streamlit標準のボタン・タブ・選択欄に、最小の高さ(44px)を指定し、
  指で押しやすい大きさにした（44pxは、スマホのタップ領域として
  一般的に推奨されている目安の大きさ）
- 画面幅640px以下のとき、タグやカロリー表示などの細かい文字が
  小さくなりすぎないよう、CSSで文字サイズを少し大きくするように調整
- スマホ幅(375px)で開くと、トップのイラスト4枚が画面の外に
  はみ出て横スクロールが発生してしまう不具合を発見・修正。
  原因は、横並び(display:flex)にした要素の中に、比率の決まった
  画像(SVG)が入っていると、その画像の比率を保とうとして
  ブラウザが要素を必要以上に大きく確保してしまう、Flexboxでよく
  知られた癖だった。該当要素に`min-width: 0`を指定して解決した
- ブラウザの開発者ツール相当の方法で、実際の要素の幅を数値で
  確認しながら検証。パソコン幅・スマホ幅(375px)のどちらでも、
  横スクロールが発生しないこと、ボタンの高さが44px以上あることを
  確認した

### 今後の作業（合意済みの残りステップ）
1. GitHubへの公開（Publicリポジトリ、.envなど機密情報は除外）
2. Streamlit Community Cloudへの登録・デプロイ
3. スマホでURLを開き、ホーム画面に追加

## 今後の展望（本人希望）
- 機能をさらに増やしたい
- 最終的にスマホアプリとして使えるようにしたい（別フェーズで方針を検討）
